import React from 'react';
import ReactDom from 'react-dom';

function Greetings(){
  return <h4>Comperari - Sales Operations Management and consulting and maybe other activities I do not know yet</h4>
};

ReactDom.render(<Greetings />, document.getElementById('root'));
