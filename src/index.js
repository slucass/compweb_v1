import React from 'react';
import ReactDom from 'react-dom';

function Greetings(){
  return <h4>Comperari - Sales Operations Management and consulting which is the best</h4>
};

ReactDom.render(<Greetings />, document.getElementById('root'));
